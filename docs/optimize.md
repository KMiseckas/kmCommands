- Optimize instance commands? Currently registered at Start() of monobehaviour. Does this use reflection? Is it heavy if many register at once? Can we instead use attribute on Monobehaviours (decided by client for which ones) that get scanned forpublic methods/properties (Not if in release mode, unless attribute marks it otherwise near method like a static one would be)? Decide whats user friendly and easy and secure for release. At runtime only need to figure out a way to match instance to function?? integration docs show this?
- Command history. Is it optimized in way snapshots are provided back? Likely unity layer will call this every time command is invoked or on refresh?? How does client know when to snapshot? Does docs for integration show this?